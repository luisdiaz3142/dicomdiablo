# Configuration backend: "file" (standalone) or "database" (shared)
# Used by systemd services when EnvironmentFile points to mercure.env
MERCURE_CONFIG_BACKEND=file

# For shared mode only (uncomment when using database backend):
# DATABASE_URL=postgresql://mercure:password@localhost/mercure
# MERCURE_CONFIG_CACHE=/opt/mercure/config/mercure.json.cache
# MERCURE_CONFIG_FILE=/opt/mercure/config/mercure.json.cache
